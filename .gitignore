# Dependencies
node_modules/

# Build outputs
.build
dist/
docs/dist/
.wrangler/
Compendus/build
Compendus/Packages/whisper.cpp/.build/

# Environment
.env
.env.local
.env.*.local
local/

# IDE
.idea/
.vscode/
*.swp
*.swo
.claude

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Runtime data
/data/
images/
xcuserdata/

# Temporary files
*.tmp
*.temp

# Test output (manually inspect generated EPUBs)
tests/output/
tests/temp/

# Playwright e2e: seeded data root + run artifacts (all regenerable)
tests/e2e/.data/
test-results/
playwright-report/
blob-report/
.playwright/

# Large GLiNER model — downloaded at runtime into data/, not committed
models/onnx-community/gliner_small-v2/onnx/

# Generated CCD packs for the iOS dev "Sample CCD" screen — regenerable via
# `pnpm ccd:fixtures`, large/churny, and kept out of release builds (a clean
# checkout has none, so the synchronized group won't bundle them).
/Compendus/Compendus/Reader/Samples/*.ccd
