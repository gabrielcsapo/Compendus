import { FleetAdminClient } from "../components/FleetAdminClient";

/** Admin → Fleet: the Idle Fleet's queue, devices, throughput, and failures. */
export default function AdminFleet() {
  return <FleetAdminClient />;
}
