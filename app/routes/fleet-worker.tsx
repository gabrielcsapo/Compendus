import { FleetWorkerClient } from "../components/FleetWorkerClient";

/** /fleet-worker — open this page on any machine to lend its CPU to the fleet. */
export default function FleetWorkerPage() {
  return <FleetWorkerClient />;
}
