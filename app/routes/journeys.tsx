import { JourneysClient } from "../components/JourneysClient";

/** Pods: compact, source-grounded learning sessions built from this library. */
export default function PodsPage() {
  return <JourneysClient />;
}
